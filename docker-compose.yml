version: "3.9"

services:
  api:
    build: .
    container_name: vehicle_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - JWT_SECRET=${JWT_SECRET}
    volumes:
      - .:/app
    restart: unless-stopped
